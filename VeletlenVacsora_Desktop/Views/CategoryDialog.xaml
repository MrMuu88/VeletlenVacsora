<Window x:Class="VeletlenVacsora.Desktop.Views.CategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VeletlenVacsora.Desktop.Views"
        mc:Ignorable="d"
		DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Category_Dialog" Width="200" SizeToContent="Height" ResizeMode="NoResize">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBox Text="{Binding Category.Name}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="5"/>
		<StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
			<Button  Width="50" Margin="5" Content="Mégse" Click="btnCancel_Click"/>
			<Button  Width="50" Margin="5" Content="Ok" Click="btnOk_Click"/>
		</StackPanel>
	</Grid>
</Window>
